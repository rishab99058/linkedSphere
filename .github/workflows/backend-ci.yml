name: Backend CI

on:
  push:
    branches:
      - develop

  pull_request:
    branches:
      - develop

jobs:
  backend-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
